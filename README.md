# mcp-easystore
a streamlined Model Context Protocol (MCP) server designed to provide LLMs with a simple, persistent key-value storage layer. It bridges the gap between ephemeral chat sessions and long-term memory by allowing AI agents to save, retrieve, and manage data across different conversations.
